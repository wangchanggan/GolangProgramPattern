package GolangProgramPattern

import (
	"testing"
)

func TestFunctionalOptions(t *testing.T) {
	//s1 := NewServer("localhost", 1024)
	//s2 := NewServer("localhost", 2048, Protocol("udp"))
	//s3:= NewServer("0.0.0.0", 8080, Timeout(300*time.Second), MaxConns(1000))
}
